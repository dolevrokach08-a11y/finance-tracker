# Claude Code local state
.claude/

# Cloudflare Wrangler cache
worker/.wrangler/

# Node / Vite — dependencies are installed locally, not committed
node_modules/

# Local backup files — git history is the source of truth
*.backup

# OS / editor cruft
.DS_Store
Thumbs.db
*.swp
.vscode/
.idea/
