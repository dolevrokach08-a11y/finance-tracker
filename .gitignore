# Claude Code local state
.claude/

# Cloudflare Wrangler cache
worker/.wrangler/

# Node / Vite — dependencies are installed locally, not committed
node_modules/
dist/

# Local copy of anything retired from the app. git history is the real
# source of truth; this is just easier to reach for.
.archive/

# Local backup files — git history is the source of truth
*.backup

# OS / editor cruft
.DS_Store
Thumbs.db
*.swp
.vscode/
.idea/

vendor-update-summary.md
