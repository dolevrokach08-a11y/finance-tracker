# תיקון חיתוך בכרטיס עמלת פירעון + אימות שגיאת הניווט

סוג: העברה  
תאריך: 2026-09-02  
ענף: `design/mortgage-entry-polish`  
מסלול: א׳ — CSS ממוקד והתנהגות ניווט משותפת.

## 1. מה שונה ולמה

בכרטיס מסלול במסך עמלת הפירעון, שורת שלושת השדות היא הפריסה הצרה ביותר במסך
הרחב. רוחב מסלול ה-grid היה 104px, אבל ריפוד של 13px מכל צד השאיר לתווית רק
76px — פחות מרוחב `יתרת קרן`, ולכן היא נחתכה באליפסיס.

ב-`shared/mortgage-lab.css` צומצם **רק** הריפוד האופקי של השורה הראשונה בכרטיסי
`#penalty-tranches` מ-13px ל-9px. מסלולי ה-grid נשארו בדיוק באותו רוחב ובאותו
יישור; רוחב התווית עלה מ-76px ל-84px וכל שלוש התוויות מוצגות במלואן.

## 2. מה נגע במספר או בזרימת נתונים

**כלום.** השינוי הוא כלל CSS יחיד. לא השתנו HTML שנוצר מהלוגיקה, ערכים, תאריכים,
חישובים, מקורות נתונים או תזמון רינדור. כל 28 בדיקות עמלת הפירעון עברו ללא שינוי.

## 3. `AbortError` — נבדק ולא שונה

הדיווח `AbortError: Transition was skipped` הוא דיווח ישן שכבר טופל ב-`188d0d0`
דרך צפייה ב-View Transition וסינון מדויק של הביטול הצפוי בלבד. שחזרתי מחדש בלשונית
נקייה את הרצף `login.html` → כניסה להדגמה → `app.html` → `mortgage.html`; הניווט
הושלם והקונסול נשאר נקי. גם בכל עשר ריצות חמשת המסכים להלן לא הופיע `AbortError`.

לכן לא הרחבתי את הסינון ולא הוספתי listener נוסף: שינוי כזה היה מטפל בתקלה שאינה
נוכחת ועלול להסתיר AbortError אמיתי של fetch.

## 4. בדיקות

- `node tests/mortgage-penalty.test.mjs` — 28/28.
- `node tools/build-assets.mjs --check` — תקין.
- `node tools/check-tailwind-coverage.mjs` — 0 מחלקות חסרות.
- `node tests/demo-isolation.test.mjs` — תקין.
- `node tests/ai-endpoint.test.mjs` — 13/13.
- מסך המשכנתא ב-1280×720: התווית 84px, ללא גלישה, קונסול נקי.
- כל חמשת המסכים ב-375×812, מצב בהיר וכהה, בלשונית הדגמה חדשה לכל מסך:
  אין גלישה אופקית ואין אזהרות או שגיאות בקונסול.
- מסך העמלה ב-375×812: השדות נשארו שתי עמודות לפי כללי המובייל, אין כיסוי של
  התוכן בידי הניווט התחתון.

## 5. מה נראה שבור ולא נגעתי

- ב-375px, הודעת ההדגמה וה-toast הזמני חופפים בזמן הטעינה במסכי השקעות ותזרים.
  זה דפוס חוצה-מסכים שאינו קשור לשינוי בכרטיס עמלת הפירעון.
- `.codex/` ו-`mockup.html` היו untracked לפני העבודה ולא נגעתי בהם.

## 6. מה לא נבדק

ארבעת המסכים שאינם משכנתא נבדקו ב-375px בלבד, בשתי ערכות הצבע. לא בוצעה להם
סקירת desktop מלאה, משום שכלל ה-CSS החדש מסונן ל-`#penalty-tranches` במשכנתא.
